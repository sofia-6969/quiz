import java.util.Scanner
public class Quiz{
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.println(" ingrese un numero para el inicio de rango: ");
        byte numInicio = scanner.nextByte();
        System.out.println(" ingrese un numero para el fin de rango: ");
        byte numFinal = scanner.nextByte();

        byte contador = 0;

        for (i = numInicio; contador <= numFinal; numFinal); {
            if (numInicio > 1) {
                
                System.out.println("numero de inicio no valido");
            } else if (condition) {
                
            }
        }
        scanner.close();
    }
}